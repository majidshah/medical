# MedVault frontend environment variables
# Copy to .env and set the backend URL — never commit .env

# Backend API base URL (no trailing slash, no /api/v1 suffix)
VITE_API_BASE_URL=http://localhost:8000
